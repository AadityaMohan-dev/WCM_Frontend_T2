import { User } from "./User";

export class Staff {
    id ? : number;
    name ? : string;
    staffCode ? : string;
    status ? : string;
    username ? : string;
    contact ? : string;
    email ? : string;
    user ? : User
}
