export class Flight{
    id ? : number;
    flightNo ? : string;
    fromDateTime ? : any;
    toDateTime ?: any;
    sourceTerminalNo ? : string;
    destinaltionTerminalNo ? : string;
    sStNumber : string;
    dStNumber : string;

}